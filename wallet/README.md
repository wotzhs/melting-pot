# Walet

This service manages the wallets.

## Getting started

### Running the service

```shell
# generate the grpc code from protobuf
make

# start the databases
docker-compose up -d

# run the service
go run cmd/app/main.go
```
